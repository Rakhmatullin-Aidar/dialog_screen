import 'dart:ui';
import 'package:flutter/material.dart';

const appBarColor = Color.fromRGBO(70, 130, 180, 1);

const bodyColor = Color.fromRGBO(176, 196, 222, 0.6);

const yourMessageColor = Color.fromRGBO(229, 255, 204, 1);

const companionMessageColor = Colors.white;

const messageStyle = TextStyle(fontSize: 17, fontWeight: FontWeight.w400, color: Colors.black);

const welcome = Text('Welcome', style: TextStyle(fontSize: 40, fontWeight: FontWeight.w700));

const emailStyle = TextStyle(fontSize: 20, fontWeight: FontWeight.w400);